const HTML = (function () {
	const main = {
		playerGrid: document.getElementById("player-grid"),
		enemyGrid: document.getElementById("enemy-grid"),
	};

	return {
		main,
	};
})();

module.exports = HTML;
